# Prompt Log
## Задание средней сложности: 1. Генерация CRUD-приложения для системы управления ЖКХ.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** "Ты — senior Python разработчик. Используя FastAPI, создай CRUD приложение для системы управления ЖКХ. Модель лицевого счета содержит id(int, PK), показания(int), начисления(int), оплаты, заявки. Эндпоинты: GET /bills/ (все счета), POST /bills/ (создать, 201), GET /bills/{id} (получить по id, 404 если нет), PUT /bills/{id} (частичное обновление, 404 если нет), DELETE /bills/{id} (удалить, 204, 404 если нет). Pydantic для валидации. Обработка ошибок. Type hints."\
**Результат:** Построена структура проекта, согласно промпту. добавлены /app/routers

### Промпт 2
**Инструмент:** OpenCode \
**Промпт:** Теперь добавь тесты (pytest) для созданного CRUD приложения. Тесты должны покрывать: создание, получение, обновление, удаление, неправильные HTTP методы. Также интеграционные тесты БД\
**Результат:** Создано тестовое покрытие. тесты проходят.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего.
- Время: ~20 мин
---


## Задание средней сложности: 2. Генерация тестов.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** сгенерируй модульные тесты (pytest) для созданного CRUD-приложения. Тесты должны покрывать: создание, чтение, обновление, удаление, а также
граничные случаи (ненайденный ID, невалидные данные). Добиться покрытия не менее
70% ключевых маршрутов.
**Результат:** Покрытие 96.8% — выше требуемых 70%. Единственный непокрытый файл database.py (строки 15-19 — это функция get_db, которая является вспомогательной и не вызывается в тестах напрямую, т.к. мы используем override_dependencies). Основные модули — crud.py, routers/bills.py, schemas.py, models.py, main.py — покрыты на 100%.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего.
- Время: ~5 мин
---


## Задание средней сложности: 3. Рефакторинг «плохого» кода.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** я добавил в crud.py намеренно плохую функцию p(). отрефактори ее с объяснением каждого изменения
**Результат:** Функция отрефакторена. Вот объяснение каждого изменения в функции p():

Что было изменено и почему
1. Имя функции p → process_monthly_accruals
Имя p ничего не говорит о назначении. Новое имя описывает бизнес-логику: обработка ежемесячных начислений.

2. Неиспользуемые параметры x2-x5 удалены, x1 переименован в period_prefix
Пять безымянных параметров, из которых 4 не использовались. Теперь один параметр с понятным именем.

3. Магические числа вынесены в константы
1.2, 150, 0.05, 5000 и т.д. разбросаны по коду. Теперь: PENALTY_RATES, DISCOUNT_AMOUNTS, COMMISSION_RATES, CRITICAL_THRESHOLD.

4. Дублирование кода устранено через helpers
Логика пеней, комиссий, скидок повторялась для каждого "типа" (type 1/2/3). Вынесена в _calculate_overdue_penalty и словари-константы.

5. Несуществующие поля (type, amount, status, penalty, period) заменены на реальные
Модель Bill не содержит этих полей. Использованы: account_number (префикс для определения типа), accruals/payments/debt. Добавлена _determine_account_type.

6. Типизация добавлена
p не имела type hints. Теперь: process_monthly_accruals(db: Session, period_prefix: str) -> MonthlyProcessingResult.

7. Переменные a, b, c, d, e переименованы
a → all_bills, b → processed_bills, c → total_debt, d → period_bills, e → total_commission.

8. Два db.commit() → один в логической группе
Было два коммита в разнобой. Теперь: первый после обработки периода, второй после маркировки критических.

9. Возврат неструктурированного кортежа (b, c, e) → MonthlyProcessingResult
Непонятно, что за числа возвращались. Теперь датакласс с именованными полями.

10. Неиспользуемая копия списка устранена
Цикл for i in a: b.append(i) просто копировал список. Убран.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего.
- Время: ~20 мин
---

## Задание средней сложности: 4. Генерация Docker-конфигурации.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** Создать Dockerfile и docker-compose.yml для проекта. Dockerfile
должен собирать приложение на FastAPI, docker-compose должен поднимать приложение
и базу данных. Учесть миграции (Alembic) и переменные
окружения для конфигурации.
**Результат:** Добавлены и сконфигурированы: Alembic миграции. Dockerfile. docker-compose.yml — два сервиса. database.py 

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего.
- Время: ~25 мин
---


## Задание средней сложности: 5. Объяснение сложного кода.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** Объясни мне данный фрагмент кода простым языком и предложи улучшения:

    def process_monthly_accruals(db: Session, period_prefix: str) -> MonthlyProcessingResult:
        all_bills = db.query(Bill).all()
        processed_bills: list[Bill] = []

    for bill in all_bills:
        account_type = _determine_account_type(bill.account_number)
        if bill.debt > 0:
            penalty = _calculate_overdue_penalty(bill.accruals, account_type)
            bill.accruals += penalty
        processed_bills.append(bill)

    total_debt = sum(b.debt for b in processed_bills)

    period_bills = db.query(Bill).filter(Bill.account_number.startswith(period_prefix)).all()
    for bill in period_bills:
        account_type = _determine_account_type(bill.account_number)
        discount = DISCOUNT_AMOUNTS.get(account_type, 50)
        bill.accruals = max(0, bill.accruals - discount)
        db.add(bill)

    db.commit()

    for bill in processed_bills:
        if bill.debt > CRITICAL_THRESHOLD:
            bill.requests = f"[CRITICAL] {bill.requests}" if bill.requests else "CRITICAL: превышен порог задолженности"
            db.add(bill)

    db.commit()

    total_commission = 0
    for bill in processed_bills:
        account_type = _determine_account_type(bill.account_number)
        rate = COMMISSION_RATES.get(account_type, 0.10)
        total_commission += int(bill.accruals * rate)

    return MonthlyProcessingResult(processed_bills, total_debt, total_commission)
**Результат:** Объяснение (простыми словами)
Функция process_monthly_accruals делает ежемесячную обработку лицевых счетов ЖКХ:

Загружает все счета из БД.
Начисляет пени на просроченные счета (где долг > 0). Тип пени зависит от типа услуги (электричество/вода/газ — определяется по префиксу номера счёта EL-, WA-, GA-).
Считает общий долг по всем счетам.
Применяет скидки для счетов, попадающих под период (например, все счета за январь с префиксом 01).
Маркирует критические счета (долг > 5000), добавляя пометку в поле requests.
Считает комиссию — процент от начислений за обработку платежей (10-15% в зависимости от типа услуги).
Возвращает обработанные счета, общий долг и общую комиссию.

Проблема:
1. Два db.commit() — если второй упадёт, первый уже зафиксирован
2. Загрузка всех записей all() — не масштабируется
3. Мутация объектов вне сессии — processed_bills содержит объекты, которые могут быть устаревшими
4. Магическая строка "CRITICAL" — хардкод в бизнес-логике
5. Два прохода по all_bills + один по processed_bills (3 цикла)
6. _determine_account_type вызывается многократно для одного счёта
7. Нет обработки ошибок БД — при падении запроса изменения частично потеряны
8. Тип возврата MonthlyProcessingResult содержит ссылки на объекты Bill после commit — lazy load может упасть

Предложения по улучшению:

1. Обернуть всё в одну транзакцию, один commit() в конце
2. Использовать пагинацию или yield_per()
3. Перезапросить после коммита или работать до коммита
4. Вынести в константу или Enum
5. Объединить логику в один проход
6. Кешировать результат на уровне счёта (мемоизация или добавление поля)
7. Обернуть в try/except с db.rollback()
8. Возвращать ID или DTO, не ORM-объекты

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего.
- Время: ~25 мин
---


## Задание средней сложности: 6. Генерация документации.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** Создай полную документацию для проекта в формате
README.md. Документация должна включать: название и описание проекта, инструкцию
по установке и запуску (с учётом Docker), описание API-эндпоинтов с примерами
запросов и ответов, описание переменных окружения.

**Результат:** Сгенерирован README.md файл согласно промпту.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: Добавление личной информации, варианта.
- Время: ~10 мин
---


## Задание средней сложности: 7. Генерация миграций БД.
### Промпт 1
**Инструмент:** OpenCode \
**Промпт:** Для модели данных нашего приложения сгенерируй SQL-миграции (с
использованием Alembic) для создания и обновления схемы базы данных.
Должны быть учтены связи между таблицами (например, один-ко-многим, многие-комногим) и индексы для часто запрашиваемых полей.

**Результат:** Созданы миграции. Проблема в том, что SQLite не работает корректно с server_default='CURRENT_TIMESTAMP' для datetime

### Промпт 2
**Инструмент:** OpenCode \
**Промпт:** SQLite не работает корректно с server_default='CURRENT_TIMESTAMP' для datetime. Исправь модели — убери server_default и используй Python для дат.

**Результат:** Миграции созданы, тесты проходят
### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: работу SQLite с server_default.
- Время: ~40 мин
---


